#include <stdio.h> /* Read the functions and symconstants defined in the standard input/output library */

int main(void) /* Define a integer function called main which expects no arguments */
{
	printf("The value of EOF is %d\n", EOF);
	/* Tell the printf function to print and format the value of EOF, which is -1 */
	return 0;
	/* Tell the main function to return 0 (No errors) */
}
